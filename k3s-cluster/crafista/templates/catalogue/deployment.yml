apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.catalogue.Name }}
  namespace: {{ .Values.catalogue.Name }}
  labels:
    app: {{ .Values.catalogue.Name }}
spec:
  replicas: {{ .Values.catalogue.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.catalogue.Name }}
  template:
    metadata:
      labels:
        app: {{ .Values.catalogue.Name }}
    spec:
      containers:
      - name: {{ .Values.catalogue.Name }}
        image: "{{ .Values.catalogue.image.repository }}:{{ .Values.catalogue.image.tag }}"
        ports:
        - containerPort: {{ .Values.catalogue.service.port }}
  env:
    - name: DB_HOST
      valueFrom:
       secretKeyRef:
         name: {{ .Values.database.name }}-secret
         key: db_host
    - name: DB_NAME
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.name }}-secret
          key: db_name
    - name: DB_USER
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.name }}-secret
          key: db_user
    - name: DB_PASSWORD
      valueFrom:
        secretKeyRef:
          name: {{ .Values.database.name }}-secret
          key: db_password
