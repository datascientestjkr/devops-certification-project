apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.catalogue.name }}
  namespace: {{ .Values.catalogue.namespace }}
  labels:
    app: {{ .Values.catalogue.name }}
spec:
  type: {{ .Values.catalogue.service.type }}
  ports:
  - port: {{ .Values.catalogue.service.port }}
    targetPort: {{ .Values.catalogue.service.port }}
  selector:
    app: {{ .Values.catalogue.name }}